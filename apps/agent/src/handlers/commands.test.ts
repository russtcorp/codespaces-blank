import { describe, it } from "vitest";

// Placeholder: command integration is exercised via higher-level agent behaviors.
describe.skip("agent commands", () => {
  it("placeholder", () => {
    // intentionally skipped to avoid build-time sql/cjs quirks during phase delivery
  });
});
